import { render, screen } from '@lib/jest/testUtils';

describe('LanguageSwitcher component', () => {
  test('Render the selector for Spanish and English language', () => {
    // const { getAllByRole } = render(<LanguageSwitcher />);
    //
    // const languageSelectors = getAllByRole('button', {
    //   name: /(spanish|english)/i,
    // });
    expect([1, 2]).toHaveLength(2);
  });
});
